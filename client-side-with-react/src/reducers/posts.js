const postsReducer = (state = {}, action) => {
    // console.log(action.type)
    switch(action.type) {
        case "UPDATE":
            return state
        default:
            return state
    }
}
export default postsReducer;